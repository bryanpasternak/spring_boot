<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.spring_boot.mapper.AdminMapper">
    <select id="selectAll" resultType="org.example.spring_boot.entity.Admin">
        SELECT * FROM admin
        <where>
            <if test="name != null">name like concat ('%', #{name}, '%')</if>
        </where>
    </select>

    <insert id="insertAdmin" parameterType="org.example.spring_boot.entity.Admin"
            useGeneratedKeys="true" keyProperty="id"> <!-- 必须加这两个属性 -->
        insert into admin (name, email, department, salary, hire_date, username, password, role)
        values (IFNULL(#{name}, '未知用户'), #{email}, #{department}, #{salary}, #{hireDate}, #{username}, #{password}, IFNULL(#{role}, 'user'))
    </insert>

    <update id="updateAdmin">
        UPDATE admin
        SET
            name = #{name},
            email = #{email},
            department = #{department},
            salary = #{salary},
            hire_date = #{hireDate}

        WHERE id = #{id}
    </update>

    <delete id="deleteAdmin">
        DELETE FROM admin
        WHERE id = #{id}
    </delete>

    <select id="selectByUsername" resultType="org.example.spring_boot.entity.Admin">
        SELECT *
        FROM admin
        WHERE username = #{username} <!-- #{username}是参数占位符，防止SQL注入 -->
    </select>

</mapper>

